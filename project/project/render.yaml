services:
  - type: web
    name: stone-realm-prototype
    env: static
    buildCommand: "echo 'Aucun build requis — site statique'"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
